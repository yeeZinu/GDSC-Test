
interface Movie {
    id: string;
    name: string;
    price: number;
}

const mockMovies: Movie[]= [
    {
        id: "aa",
        name: "dkqhfdk",
        price: 1000,
    }
]

function useMovie() {
    return {};
}

export default useMovie;